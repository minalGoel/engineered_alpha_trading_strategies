"""Hand-implemented strategy signal generators.

Each strategy file exports a `Strategy` class that inherits from `BaseStrategy`
and implements the `compute()` method to produce entry/exit signals from OHLCV data.
"""
