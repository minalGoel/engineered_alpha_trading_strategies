{
  "name": "bayesian_update_v1",
  "version": "0.1.0",
  "author": "claude-4.6-opus-agent-4",
  "tags": [
    "bayesian",
    "probability",
    "updating",
    "adaptive",
    "quantitative"
  ],
  "thesis": "Markets provide a stream of evidence (price movements, volume, spread changes) that can be used to update the probability of a stock trending up or down in the next 30 minutes via Bayesian updating. Starting with a prior based on NIFTY direction and sector strength, each 1-min bar's return, volume, and VWAP position provides likelihood evidence that updates the posterior probability. We trade when the posterior exceeds 0.70 (strong directional conviction). The inefficiency exists because Bayesian updating requires explicit probability modeling, which is foreign to most retail and many institutional intraday traders. The edge comes from formally aggregating heterogeneous evidence rather than relying on ad-hoc indicator combinations.",
  "universe": "NIFTY 50 constituents",
  "stock_filter": "ADV(20) > ₹100 Cr; at least 60 days of historical 1-min data for prior calibration",
  "timeframe": "1min",
  "session": "09:30-15:10 IST",
  "hold_time": "15-45 min",
  "max_hold_bars": 60,
  "trades_per_day": "3-6",
  "max_trades_per_day": 8,
  "data": {
    "ohlcv_1min": true,
    "vwap": true,
    "vix": true,
    "index": "NIFTY 50",
    "option_chain": false,
    "order_book": false,
    "other": [
      "sector_index_1min",
      "historical_1min_60d"
    ]
  },
  "indicators": [
    {
      "name": "Prior_Prob",
      "params": "0.5 + 0.1 * sign(NIFTY_ROC_30) + 0.05 * sign(Sector_ROC_30)",
      "lookback": 30
    },
    {
      "name": "Price_Likelihood",
      "params": "P(bar_return | trend_up) from historical distribution of 1-min returns",
      "lookback": 1
    },
    {
      "name": "Volume_Likelihood",
      "params": "P(volume_ratio | trend_up) where volume_ratio = volume / SMA(volume,20)",
      "lookback": 20
    },
    {
      "name": "VWAP_Likelihood",
      "params": "P(VWAP_distance | trend_up) from historical VWAP deviation distributions",
      "lookback": "session"
    },
    {
      "name": "Posterior_Prob",
      "params": "Bayesian update: Prior * Product(Likelihoods) / Normalizer, updated every bar",
      "lookback": "rolling"
    }
  ],
  "entry": {
    "long": {
      "conditions": [
        "Posterior_Prob(trend_up) > 0.70",
        "Posterior_Prob has been monotonically increasing for last 3 bars",
        "close > vwap"
      ],
      "confirmation": "Posterior_Prob(trend_up) > 0.70 sustained for 2 consecutive bars"
    },
    "short": {
      "conditions": [
        "Posterior_Prob(trend_up) < 0.30 (i.e., Posterior_Prob(trend_down) > 0.70)",
        "Posterior_Prob has been monotonically decreasing for last 3 bars",
        "close < vwap"
      ],
      "confirmation": "Posterior_Prob(trend_up) < 0.30 sustained for 2 consecutive bars"
    },
    "entry_price": "market order after 2-bar confirmation"
  },
  "exit": {
    "target": "35 bps from entry",
    "stop_loss": "20 bps from entry",
    "trailing_stop": "After 20 bps profit, trail at 12 bps from high-water mark",
    "time_stop": "60 bars (1 hour)",
    "eod_rule": "flatten",
    "signal_exit": "Posterior_Prob crosses 0.50 against position direction"
  },
  "risk": {
    "capital_per_trade": 100000,
    "max_risk_per_trade_pct": 0.2,
    "position_sizing": "Scale with conviction: ₹75K if Posterior 0.70-0.75, ₹1L if 0.75-0.85, ₹1.25L if > 0.85",
    "max_open_positions": 2,
    "max_daily_loss_inr": 3000,
    "max_portfolio_heat_pct": 0.6
  },
  "filters": {
    "vix_filter": "India VIX < 25; recalibrate likelihood distributions if VIX > 20",
    "time_filter": "No entries before 09:30 (prior calibration needs initial bars) or after 15:10",
    "volume_filter": "Volume_Likelihood must not be degenerate (volume > 100 shares per bar)",
    "trend_filter": "None — Bayesian framework naturally incorporates trend information via prior",
    "spread_filter": "Bid-ask spread < 5 bps",
    "event_filter": "Skip stock on earnings day — likelihood distributions not calibrated for event-day volatility",
    "other_filters": [
      "Posterior must not be stuck at 0.50 ± 0.05 for > 30 bars (no information arriving)"
    ]
  },
  "edge": {
    "expected_bps": 22,
    "win_rate_estimate": 0.59,
    "avg_winner_to_loser": 1.0,
    "profit_factor_estimate": 1.44,
    "edge_decay_risk": "Low-Medium — Bayesian updating framework is novel for intraday trading; calibration of likelihoods from historical data is the moat"
  },
  "costs": {
    "slippage_bps_per_side": 3,
    "brokerage": "zerodha_intraday",
    "stt_bps": 2.5,
    "exchange_charges_bps": 0.5
  },
  "weaknesses": [
    "Likelihood distributions must be calibrated from historical data and are assumed stationary — market regime changes invalidate the calibration, requiring periodic recalibration (at least monthly)",
    "Bayesian updating with multiple likelihoods assumes conditional independence between price, volume, and VWAP evidence, which is clearly violated (high volume and large price moves are correlated)",
    "The prior based on NIFTY and sector direction introduces look-ahead if not carefully lagged; must use strictly prior-bar values to avoid data leakage"
  ],
  "prior_art": "Bayesian methods in finance: Johannes & Polson (2010); West & Harrison (1997) dynamic linear models; Bayesian portfolio allocation (Black-Litterman); application to intraday entry timing is less explored",
  "notes": "Consider using a particle filter or sequential Monte Carlo instead of closed-form Bayesian updates to handle non-Gaussian likelihoods. The likelihood distributions should be estimated using kernel density estimation from the last 60 trading days."
}