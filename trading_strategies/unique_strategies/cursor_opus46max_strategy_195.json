{
  "name": "lasso_feature_select_v1",
  "version": "0.1.0",
  "author": "claude-4.6-opus-agent-4",
  "tags": [
    "LASSO",
    "feature-selection",
    "regularization",
    "machine-learning",
    "quantitative"
  ],
  "thesis": "With dozens of potential indicators (RSI, MACD, BB, VWAP, volume ratios, etc.), overfitting is the primary risk. LASSO (Least Absolute Shrinkage and Selection Operator) regression automatically selects the most predictive features by shrinking unimportant coefficients to zero. By fitting a LASSO model on the last 5 days of 1-min data with the target variable being the 20-bar forward return and 15 potential features, the non-zero coefficients identify which indicators are currently predictive. We trade only when the LASSO-selected features collectively predict a forward return exceeding the cost threshold. The inefficiency exists because most traders either use too few indicators (missing information) or too many (overfitting). LASSO finds the optimal sparse set.",
  "universe": "NIFTY 50 constituents",
  "stock_filter": "ADV(20) > \u20b9100 Cr; at least 5 days of complete 1-min data for LASSO training",
  "timeframe": "1min",
  "session": "09:30-15:10 IST",
  "hold_time": "15-30 min",
  "max_hold_bars": 45,
  "trades_per_day": "3-6",
  "max_trades_per_day": 7,
  "data": {
    "ohlcv_1min": true,
    "vwap": true,
    "vix": true,
    "index": "NIFTY 50",
    "option_chain": false,
    "order_book": false,
    "other": [
      "5d_historical_1min_features"
    ]
  },
  "indicators": [
    {
      "name": "F1_VWAP_Dist",
      "params": "(close - vwap) / vwap * 10000",
      "lookback": "session"
    },
    {
      "name": "F2_RSI_14",
      "params": "RSI(close, 14)",
      "lookback": 14
    },
    {
      "name": "F3_MACD_Hist",
      "params": "MACD(close,12,26,9) - MACD_signal(close,12,26,9)",
      "lookback": 33
    },
    {
      "name": "F4_BB_Position",
      "params": "(close - BB_lower) / (BB_upper - BB_lower) where BB(20,2)",
      "lookback": 20
    },
    {
      "name": "F5_Volume_Ratio",
      "params": "volume / SMA(volume, 20)",
      "lookback": 20
    },
    {
      "name": "F6_ATR_Norm",
      "params": "ATR(14) / close * 10000",
      "lookback": 14
    },
    {
      "name": "F7_OBV_Slope",
      "params": "linreg_slope(OBV, 20)",
      "lookback": 20
    },
    {
      "name": "F8_NIFTY_Return_20",
      "params": "ROC(NIFTY_50_close, 20)",
      "lookback": 20
    },
    {
      "name": "F9_VIX_Level",
      "params": "India_VIX",
      "lookback": "real-time"
    },
    {
      "name": "F10_Momentum_10",
      "params": "ROC(close, 10)",
      "lookback": 10
    },
    {
      "name": "LASSO_Prediction",
      "params": "Lasso(alpha=0.001).predict([F1..F10]) \u2014 predicted 20-bar forward return",
      "lookback": "model fitted on 5d history"
    },
    {
      "name": "Active_Features",
      "params": "count of non-zero LASSO coefficients",
      "lookback": "model fitted on 5d history"
    }
  ],
  "entry": {
    "long": {
      "conditions": [
        "LASSO_Prediction > 15 bps (predicted forward return exceeds cost threshold)",
        "Active_Features >= 3 (at least 3 features are predictive \u2014 not degenerate model)",
        "Active_Features <= 7 (not too many \u2014 prevents overfitting signal)",
        "close > vwap"
      ],
      "confirmation": "LASSO_Prediction has been > 10 bps for 3 consecutive bars (stable prediction)"
    },
    "short": {
      "conditions": [
        "LASSO_Prediction < -15 bps",
        "Active_Features >= 3 AND Active_Features <= 7",
        "close < vwap"
      ],
      "confirmation": "LASSO_Prediction has been < -10 bps for 3 consecutive bars"
    },
    "entry_price": "market order on confirmation"
  },
  "exit": {
    "target": "abs(LASSO_Prediction) bps from entry (model's expected return as target)",
    "stop_loss": "15 bps from entry (tight \u2014 if model is wrong, exit quickly)",
    "trailing_stop": "After 15 bps profit, trail at 8 bps from high-water mark",
    "time_stop": "45 bars (model predicts 20-bar return; allow 2x buffer)",
    "eod_rule": "flatten",
    "signal_exit": "LASSO_Prediction sign flips (model now predicts opposite direction)"
  },
  "risk": {
    "capital_per_trade": 100000,
    "max_risk_per_trade_pct": 0.15,
    "position_sizing": "Scale with abs(LASSO_Prediction): \u20b975K if 15-25 bps, \u20b91L if 25-40 bps, \u20b91.25L if > 40 bps",
    "max_open_positions": 2,
    "max_daily_loss_inr": 2500,
    "max_portfolio_heat_pct": 0.5
  },
  "filters": {
    "vix_filter": "India VIX < 25; re-train LASSO model if VIX regime changes significantly",
    "time_filter": "No entries before 09:30 or after 15:10",
    "volume_filter": "Volume > SMA(volume, 20)",
    "trend_filter": "None \u2014 LASSO model captures trend through F8 (NIFTY return) and F10 (momentum)",
    "spread_filter": "Bid-ask spread < 5 bps",
    "event_filter": "Skip stock on earnings day; exclude event-day data from LASSO training",
    "other_filters": [
      "LASSO model R\u00b2 on training data must be > 0.02 (model has minimum predictive power)"
    ]
  },
  "edge": {
    "expected_bps": 26,
    "win_rate_estimate": 0.55,
    "avg_winner_to_loser": 1.2,
    "profit_factor_estimate": 1.47,
    "edge_decay_risk": "Medium \u2014 LASSO is well-known in ML; the edge is in the specific feature set and real-time application; model requires daily re-training to stay current"
  },
  "costs": {
    "slippage_bps_per_side": 3,
    "brokerage": "zerodha_intraday",
    "stt_bps": 2.5,
    "exchange_charges_bps": 0.5
  },
  "weaknesses": [
    "LASSO assumes linear relationships between features and forward returns \u2014 non-linear interactions (e.g., RSI matters only when VIX is high) are missed, potentially leaving alpha on the table",
    "The alpha regularization parameter (0.001) is fixed but should ideally be cross-validated \u2014 too high zeroes out important features, too low loses regularization benefit and overfits",
    "R\u00b2 > 0.02 threshold for model quality is extremely low \u2014 a model that explains only 2% of return variance may not generate reliable predictions despite passing the threshold"
  ],
  "prior_art": "Tibshirani (1996) LASSO; Gu, Kelly & Xiu (2020) 'Empirical Asset Pricing via Machine Learning'; LASSO for feature selection in trading is increasingly common in quant research; Moritz & Zimmermann (2016) on regularized regression for cross-sectional return prediction",
  "notes": "Consider using Elastic Net (L1 + L2 regularization) instead of pure LASSO for better handling of correlated features (many of our features like RSI, BB_Position, Momentum are correlated). Use time-series cross-validation (walk-forward) instead of standard CV to prevent look-ahead bias. Retrain the model at least daily.",
  "_filepath": "all_trading_strategies/cursor_opus46max/cursor_opus46max_strategy_195.json",
  "_dedup_metadata": {
    "original_path": "all_trading_strategies/cursor_opus46max/cursor_opus46max_strategy_195.json",
    "phase1_survived": true,
    "phase2_survived": true,
    "phase3_survived": true,
    "was_duplicate_kill": false,
    "family": null,
    "num_within_family_comparisons": 0,
    "variant_pairs": [],
    "different_pairs": []
  }
}